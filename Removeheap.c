#include <stdio.h>
#include <stdlib.h>

Typedef struct aheap{
 Int max;
 Int pos;
 Float* prioridade;
 } Heap;

Static void corrige_abaixo(Heap* heap_hurra){
  Int pai=0;
  

Float heap_remove(Heap* heap_hurra){
  If(heap->
     
 A funcao imprime na tela as 3 maiores prioridade obs estao disponiveis as funcoes cria, insire, retira, vazio, cheio.
 B considerando q n ha nenhuma func disponivel, crie uma funcao q retorne a menor prioridade obs aproveitar ordenacao da lista
 
