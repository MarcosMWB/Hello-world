#include <stdio.h>
#include <stdlib.h>

Typedef struct _heap{
 Int max;
 Int pos;
 Float* prioridade;
 } Heap;
 
Heap* newheap(int max){
 Heap* heap=(Heap*)malloc(sizeof(Heap));
 Heap->max=max;
 Heap->pos=0;
 Heap->prioridade=(float*)
}
